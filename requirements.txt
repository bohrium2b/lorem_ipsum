# Core
transformers>=4.44.0
safetensors>=0.4.3
accelerate>=0.33.0

# Tokenizers/utilities commonly needed by HF models
sentencepiece>=0.1.99

# PyTorch (install the variant appropriate for your platform/GPU)
# You may install torch via this file, or follow the official install guide:
#   https://pytorch.org/get-started/locally/
torch>=2.3.0